%%% -*- mode:erlang -*-
%%% Warning - this config file *must* end with <dot><whitespace>

[{resource_discovery,
  [{contact_nodes,       ['rd@localhost']},
   {heartbeat_frequency, 60000},
   {log4erl_config,      "etc/log4erl.conf"}]}].
